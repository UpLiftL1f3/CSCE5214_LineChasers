For starting the backend:
    1) Start a virtual env: python3 -m venv venv
    2) activate it: source venv/bin/activate
    3) Get Flask & Cors: pip install Flask flask-cors
    4) Run Backend: flask run
